# Copia este archivo a ".env" solo si quieres apuntar tu entorno local a un
# backend remoto en vez de usar el proxy de Vite hacia localhost:8000.
# En Vercel, esta variable se configura en Project Settings > Environment Variables.

VITE_BACKEND_ORIGIN=https://tu-backend.onrender.com
